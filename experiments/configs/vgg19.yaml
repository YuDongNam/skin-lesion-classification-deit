name: vgg19
model_name: keras/vgg19  # placeholder note
image_size: 224
data_dir: data
output_dir: experiments/outputs/vgg19
use_wandb: false

train:
  batch_size: 32
  transfer_epochs: 10
  finetune_epochs: 20
